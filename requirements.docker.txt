# ============================================================
#  AttendX — Python Dependencies for Docker
#  (dlib is pre-installed separately in the Dockerfile)
# ============================================================

# Web framework
fastapi==0.110.0
uvicorn[standard]==0.29.0

# Face recognition (dlib must already be installed)
face_recognition==1.3.0

# Computer vision (headless — no GUI/X11 required)
opencv-python-headless==4.9.0.80

# Image processing
Pillow==10.3.0

# Data & Excel
pandas==2.2.1
numpy==1.26.4
openpyxl==3.1.2

# Firebase
firebase-admin==6.5.0
google-cloud-firestore==2.16.0
